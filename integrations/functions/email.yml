functions:
  sendEmail:
    handler: handlers/emailHandler.sendEmailHandler
    events:
      - httpApi:
          path: integrations/send-email
          method: post
          authorizer:
            type: jwt
            name: EsenciaAuthorizer

functions:
  sendEmail:
    handler: handlers/emailHandler.sendEmailController
    events:
      - httpApi:
          path: /integrations/send-email
          method: post
          authorizer:
            type: jwt
            id: osl69b

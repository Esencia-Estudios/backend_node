import { encrypt } from "../helpers/encryption.js";

const passwordAEncriptar = "";
const claveSecreta = "";

const valorEncriptado = encrypt(passwordAEncriptar, claveSecreta);
console.log("Valor encriptado: ", valorEncriptado);

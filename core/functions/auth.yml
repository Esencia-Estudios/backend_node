functions:
  force-change-password:
    handler: handlers/authHandler.forceChangePassword
    events:
      - httpApi:
          path: /auth/force-change-password
          method: post

  login-user:
    handler: handlers/authHandler.loginUser
    events:
      - httpApi:
          path: /auth/login
          method: post